{
  "name": "@avwie/architecture",
  "private": true,
  "version": "1.0.0",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@nestjs/common": "*",
    "express": "*"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rimraf dist",
    "test": "jest --verbose --runInBand"
  }
}
